<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cindodcindy.vieroshoes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VieroShoes">
        <activity android:name=".view.ListSepatu"></activity>
        <activity android:name=".view.MenuBaruActivity" />
        <activity android:name=".view.BottomNavActivity" />
        <activity android:name=".view.Chat_page" />
        <activity android:name=".view.Order_page" />
        <activity android:name=".view.Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>